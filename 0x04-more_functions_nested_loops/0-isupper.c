#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 *
 * Description: function that checks for uppercase
 *
 * Return: Always 0
 */

int _isupper(int c)
{
	int upper;

	if (upper >= 65 && upper <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
